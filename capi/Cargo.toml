[package]
name = "agent_mem_db_capi"
version = "0.1.0"
edition = "2021"
description = "C API for AgentMemDB — used by Go bindings"

[lib]
crate-type = ["cdylib"]

[dependencies]
agent_mem_db = { path = ".." }
serde_json = "1.0"
libc = "0.2"
